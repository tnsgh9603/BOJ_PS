#include <bits/stdc++.h>

#define fastio ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
using namespace std;

int main() {
    fastio;
    int tc;
    cin >> tc;
    for (int i = 0; i < tc; ++i) {
        int a, b;
        cin >> a >> b;
        cout << b << " 5\n";
    }
    return 0;
}