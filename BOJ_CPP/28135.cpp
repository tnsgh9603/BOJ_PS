#include <bits/stdc++.h>

#define fastio ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
using namespace std;

int main() {
    fastio;
    int n, cnt = 0, now = 0;
    cin >> n;
    while(n != now) {
        if(now == 50)
        ++now;
    }
    return 0;
}