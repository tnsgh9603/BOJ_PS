#include <bits/stdc++.h>

#define fastio ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
using namespace std;

int main() {
    fastio;
    int k, n;
    cin >> k >> n;
    vector<int> v(k);
    for (int i = 0; i < k; ++i) {
        cin >> v[i];
    }
    return 0;
}