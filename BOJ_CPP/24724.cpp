#include <bits/stdc++.h>

#define fastio ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
using namespace std;

int main() {
    fastio;
    int t, cnt = 0;
    cin >> t;
    while(t--) {
        int n;
        cin >> n;
        ++n;
        while(n--) {
            int a, b;
            cin >> a >> b;
        }
        cout << "Material Management " << ++cnt << "\nClassification ---- End!\n";
    }
    return 0;
}